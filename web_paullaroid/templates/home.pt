<tal:block metal:use-macro="load: layout.pt">        
    
    <nav metal:fill-slot="navigo">
        <a href="${request.route_path('home')}">Home</a>
    </nav>

    <div id="main" metal:fill-slot="main">
    <ul tal:repeat="event events">
            <li>
                <p>
                    il y a actuellement ${event.value['nb']} photos dans cette
galerie <a
                    href="${request.route_path('event',event=event.key[0])}">${event.key[0]}</a></p>
            </li>
        </ul>
    </div>
</tal:block>
